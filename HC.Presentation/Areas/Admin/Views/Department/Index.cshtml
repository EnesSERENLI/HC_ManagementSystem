@model List<DepartmentVM>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <section class="panel">
            <header class="panel-heading">
                Depmartments
            </header>
            <div class="alert-error text-success">
                @if (TempData["message"] != null)
                {
                    <div class="alert alert-success alert-block fade in">
                        <button data-dismiss="alert" class="close close-sm" type="button">
                            <i class="icon-remove"></i>
                        </button>
                        <h4>
                            <i class="icon-ok-sign"></i>
                            Success!
                        </h4>
                        <p>@TempData["message"]</p>
                    </div>
                }
            </div>
            <table class="table table-striped table-advance table-hover">
                <thead>
                    <tr>
                        <th><i class="icon-bullhorn"></i> DepartmentName</th>
                        <th><i class=" icon-edit"></i> Status</th>
                        <th><a asp-action="AddDepartment" class="btn btn-primary btn-sm">Add Department</a></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var department in Model)
                    {
                        <tr>
                            <td class="hidden-phone">@department.DepartmentName</td>
                            <td>
                                @if (department.Status == HC.Domain.Enums.Status.Active)
                                {
                                    <span class="label label-success label-mini">@department.Status</span>
                                }
                                else if (department.Status == HC.Domain.Enums.Status.Deleted)
                                {
                                    <span class="label label-danger label-mini">@department.Status</span>
                                }
                                else if (department.Status == HC.Domain.Enums.Status.Updated)
                                {
                                    <span class="label label-warning">@department.Status</span>
                                }

                            </td>
                            <td>
                                <a asp-area="admin" asp-action="UpdateDepartment" asp-route-id="@department.ID" class="btn btn-warning btn-xs icon-pencil">Update</a>
                                <a asp-area="admin" asp-action="DeleteDepartment" asp-route-id="@department.ID" class="btn btn-danger btn-xs icon-trash">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </section>
    </div>
</div>

